
<?php echo $this->tag->form("car_model/create") ?>

<table width="100%">
    <tr>
        <td align="left"><?php echo $this->tag->linkTo(array("car_model", "Go Back")) ?></td>
        <td align="right"><?php echo $this->tag->submitButton("Save") ?></td>
    </tr>
</table>

<?php echo $this->getContent(); ?>

<div align="center">
    <h1>Create car_model</h1>
</div>

<table>
    <tr>
        <td align="right">
            <label for="name">Name</label>
        </td>
        <td align="left">
            <?php echo $this->tag->textField(array("name", "size" => 30)) ?>
        </td>
    </tr>
    <tr>
        <td align="right">
            <label for="FK_CAR_MAKE">Fabricante</label>
        </td>
        <td align="left">
            <select name="FK_CAR_MAKE" id="FK_CAR_MAKE" class="form-control">
                <option>Selecciona un fabricante</option>
                <?php 
                foreach ($car_makes as $make) {
                    echo "<option value='" . $make->ID_CAR_MAKE . "'>$make->name</option>";
                }
                ?>
            </select>
        </td>
    </tr>

    <tr>
        <td></td>
        <td><?php echo $this->tag->submitButton("Save") ?></td>
    </tr>
</table>

</form>
